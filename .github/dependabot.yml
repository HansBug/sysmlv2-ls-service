version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    groups:
      test-tooling:
        patterns:
          - "vitest"
          - "vite"
          - "esbuild"
          - "tsx"
      lint-tooling:
        patterns:
          - "eslint"
          - "typescript-eslint"
    ignore:
      # TypeScript major upgrades are handled as deliberate migrations because
      # the current pinned upstream/tooling stack is validated on TypeScript 5.
      - dependency-name: "typescript"
        update-types:
          - "version-update:semver-major"

  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5

  - package-ecosystem: "docker"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 3
    ignore:
      # Keep the published image on the current Node LTS major. Adopt the next
      # LTS major deliberately after compatibility and upstream smoke testing.
      - dependency-name: "node"
        update-types:
          - "version-update:semver-major"
