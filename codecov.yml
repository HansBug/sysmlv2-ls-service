comment:
  layout: "reach,diff,flags,tree"
  behavior: default

flags:
  python-client:
    paths:
      - clients/python/src/

coverage:
  status:
    project:
      default:
        target: auto
    patch:
      default:
        target: 100%
