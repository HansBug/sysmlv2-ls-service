mkdocs>=1.6,<2
mkdocs-material>=9.5,<10
mkdocstrings[python]>=0.25,<1
# Keep build backends pinned because Read the Docs installs the Python client
# with --no-build-isolation before running MkDocs.
setuptools>=83.0.0,<84
wheel<0.42
