# Clients

This repository currently ships a Python client under `clients/python` and keeps `examples/python` as a separate stdlib smoke example. Future clients can be added under `clients/<language>` without changing the service API boundary.
