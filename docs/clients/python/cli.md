# Python CLI

The CLI uses Click subcommands and supports both `--help` and `-h`. See the generated [CLI reference](../../generated/cli/index.md).

Common commands:

```bash
sysmlv2sl health
sysmlv2sl version
sysmlv2sl validate text 'package Demo {}'
sysmlv2sl validate directory . --include '**/*.sysml'
```
