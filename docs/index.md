# sysmlv2-ls-service documentation

`sysmlv2-ls-service` is a Docker-ready TypeScript HTTP wrapper around the pinned Sensmetry `sysml-2ls` SysML v2 / KerML language-server core. It exposes syntax, linking, and semantic validation through service-owned DTOs and a small HTTP API.

This site is versioned on Read the Docs and covers only this repository's service code, generated API references, Python client, CLI, Docker behavior, and upstream-derived inventory. It does not publish the upstream source tree.

## Start here

- [Quickstart](getting-started/quickstart.md) for a local validation request.
- [HTTP API](service/http-api.md) for endpoints and DTO boundaries.
- [Python client](clients/python/install.md) for SDK and CLI usage.
- [Upstream inventory](reference/upstream/index.md) for generated capability maps from the pinned submodule.
